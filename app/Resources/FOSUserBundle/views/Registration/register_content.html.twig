{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
    {{ form_widget(form) }}
    <div class="form-group ">
        <input type="submit" value="{{ 'registration.submit'|trans }}" class="btn c-theme-btn c-btn-uppercase btn-lg c-btn-bold c-btn-square btn-block" id="submit"/>
    </div>
<style>
    #submit{
        margin-top: 20px;
    }
    #captchacode{

    }
</style>
{{ form_end(form) }}